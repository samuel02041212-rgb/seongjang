---
description: seongjang web accent #ffcd38
globs: web/**/*.tsx, web/**/*.css
---

- Accent `#ffcd38`: CTA, active links, focus — use `globals.css` `--accent` / Tailwind `bg-accent`, `text-accent-foreground`, `bg-accent-soft`.
- Do not use random yellows; stick to these tokens.
